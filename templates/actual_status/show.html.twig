{% extends 'base.html.twig' %}

{% block title %}ActualStatus{% endblock %}

{% block body %}
    <h1>ActualStatus</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ actual_status.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ actual_status.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('actual_status_index') }}">back to list</a>

    <a href="{{ path('actual_status_edit', {'id': actual_status.id}) }}">edit</a>

    {{ include('actual_status/_delete_form.html.twig') }}
{% endblock %}
