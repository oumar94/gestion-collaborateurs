{% extends 'base.html.twig' %}

{% block title %}Collaborator{% endblock %}

{% block body %}
    <h1>Collaborator</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ collaborator.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ collaborator.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ collaborator.prenom }}</td>
            </tr>
            <tr>
                <th>Date_dispo</th>
                <td>{{ collaborator.dateDispo ? collaborator.dateDispo|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Date_depart_prevue</th>
                <td>{{ collaborator.dateDepartPrevue ? collaborator.dateDepartPrevue|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Filename</th>
                <td>{{ collaborator.filename }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('collaborator_index') }}">back to list</a>

    <a href="{{ path('collaborator_edit', {'id': collaborator.id}) }}">edit</a>

    {{ include('collaborator/_delete_form.html.twig') }}
{% endblock %}
