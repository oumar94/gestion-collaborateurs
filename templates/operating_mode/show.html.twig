{% extends 'base.html.twig' %}

{% block title %}OperatingMode{% endblock %}

{% block body %}
    <h1>OperatingMode</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ operating_mode.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ operating_mode.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('operating_mode_index') }}">back to list</a>

    <a href="{{ path('operating_mode_edit', {'id': operating_mode.id}) }}">edit</a>

    {{ include('operating_mode/_delete_form.html.twig') }}
{% endblock %}
