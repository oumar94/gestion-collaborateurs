<form method="post" action="{{ path('operating_mode_delete', {'id': operating_mode.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ operating_mode.id) }}">
    <button class="btn">Delete</button>
</form>
